[tool.ruff]
line-length = 100
target-version = "py38"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "C4", "SIM", "C9"]
ignore = [
    "BLE001",  # 配置读取/后台线程刻意捕获盲异常
    "DTZ005",  # 报告时间用本地时区
]

[tool.ruff.format]
quote-style = "double"
